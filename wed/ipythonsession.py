# coding: utf-8
improt boto3
import boto3
session = boto3.Session(profile_name='pythonauto')
s3 = session.resourses('s3')
s3 = session.resourse('s3')
s3 = session.resource('s3')
bucket in s3.bucket.all
bucket in s3.buckets.all
for bucket in s3.buckets.all
for bucket in s3.buckets.all();
for bucket in s3.buckets.all():
    print(bucket)
    
for bucket in s3.buckets.all():
    print(bucket)
    
for bucket in s3.buckets.all():
    print(bucket)
    
new_bucket = s3.create_bucket(Bucket='autoaws-boto3')
new_bucket = s3.create_bucket(Bucket='autoaws-boto3',CreateBucketConfigurtion = `{'LocationConstaint' : 'us-east-1'})
new_bucket = s3.create_bucket(Bucket='autoaws-boto3',CreateBucketConfiguration = `{'LocationConstaint' : 'us-east-1'})
new_bucket = s3.create_bucket(Bucket='autoaws-boto3',CreateBucketConfiguration ={'LocationConstaint' : 'us-east-1'})
new_bucket = s3.create_bucket(Bucket='autoaws-boto3',CreateBucketConfiguration ={'LocationConstraint' : 'us-east-1'})
new_bucket = s3.create_bucket(Bucket='autoaws-boto3',CreateBucketConfiguration ={'LocationConstraint' : 'us-east-2'})
for bucket in s3.buckets.all():
    print(bucket)
    
get_ipython().run_line_magic('history', '')
get_ipython().run_line_magic('save', 'ipython.py')
get_ipython().run_line_magic('save', 'ipythonsession.py')
get_ipython().run_line_magic('save', 'ipythonsession.py 1-50')
get_ipython().run_line_magic('save', 'ipythonsession.py 1-60')
